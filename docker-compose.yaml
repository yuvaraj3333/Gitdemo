version: '3'
services:
  web:
    image: nginx:latest
    ports:
      - "8080:80"
  app:
    build: ./app
    ports:
      - "5000:5000"
    depends_on:
      - db
  db:
    image: postgres:13
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: mydb
